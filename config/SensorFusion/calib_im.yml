# Interaction Matrix and Reconstructor Calibration
# ================================================

# Push-pull command generator
pushpull:
  class:        'PushPullGenerator'
  nmodes:       1851                         # Number of DM actuators
  amp:          10   
  constant_amp: true
  outputs:   ['output']

# Interaction matrix calibrator
pyr_im_calibrator:
  class:     'ImCalibrator'
  nmodes:    1851                         # Number of modes to calibrate
  im_tag:    'pyr0.0_1851modes_im'            # Output IM filename
  overwrite: true                         # Overwrite existing files
  inputs:
    in_slopes:   'pyr_slopes.out_slopes'      # WFS slopes input
    in_commands: 'pushpull.output'        # Push-pull commands

zwfs_im_calibrator:
  class:     'ImCalibrator'
  nmodes:    1851                         # Number of modes to calibrate
  im_tag:    'z1.0wfs_1851modes_im'            # Output IM filename
  overwrite: true                         # Overwrite existing files
  inputs:
    in_slopes:   'zwfs_slopes.out_slopes'      # WFS slopes input
    in_commands: 'pushpull.output'        # Push-pull commands

# Override main simulation parameters
main_override:
  time_step: 0.0005
  total_time: 1.851                 # 1851 modes × 2 (push+pull) × 0.0005s

# Disable atmosphere for clean calibration
prop_override:
  source_dict_ref:   ['source_ngs']
  inputs:
    common_layer_list: ['vlt_aperture', 'dm.out_layer']  # Only pupil + DM
  outputs:           ['out_source_ngs_ef']

# Override DM to use calibration commands
dm_override:
  sign: 1                                 # Use positive sign for calibration (default is -1)
  nmodes: 1851                            # Use all modes for calibration
  inputs:
    in_command: 'pushpull.output'         # Connect to push-pull generator

pyr_override:
  mod_amp: 0.0
  inputs: 
    in_ef: 'prop.out_source_ngs_ef'

zwfs_override:
  spot_radius_lambda: 0.5                     # Spot RADIUS in λ/D units
  # phase_shift:        1.5707963267948966      # Spot phase delay (pi/2)
  inputs: 
    in_ef: 'prop.out_source_ngs_ef'

# Disable noise for clean measurements
cred1_override:
  photon_noise:   false                   # No photon noise
  readout_noise:  false                   # No read noise
  excess_noise:   false                   # No excess noise

ocam2_override:
  photon_noise:   false                   # No photon noise
  readout_noise:  false                   # No read noise
  excess_noise:   false                   # No excess noise

# Remove unnecessary objects during calibration
remove: ['atmo','seeing','wind_speed','wind_direction','data_store','atmo_mode_res', #'source_science',
        'dm_mode_res','filter','psf', 'ef_prop','pyr_modalrec', 'zwfs_modalrec']
